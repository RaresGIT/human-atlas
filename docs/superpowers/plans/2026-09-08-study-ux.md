# Study UX Implementation Plan

> **For agentic workers:** Use superpowers:subagent-driven-development for independent activity presentation and final review; implement the coupled workspace and camera changes together.

**Goal:** Apply the approved Study redesign while retaining every tool and information view.
**Architecture:** Existing state and persistence remain in StudyWorkspace. Extract presentation components for browser, inspector and scene tools; track transient activity, tool, inspector and sheet state outside persisted workspace. Share layout dimensions between CSS and camera reservations.
**Tech Stack:** React 19, TypeScript, Vite, existing shadcn controls, Three.js, Playwright.
**Spec:** docs/superpowers/specs/2026-09-08-study-ux-design.md

## Global constraints
- Preserve all anatomy, practice, review, library, lesson, import/export, keyboard and scene functionality.
- Desktop, narrow portrait and short landscape must be usable; keyboard focus and reduced motion respected.
- Keep existing user edits and Reset fix; no deployment or push.

## Tasks
- [x] 1. Establish browser regression harness. Add Playwright to development tooling, test local app entry and expected stable browser/inspector behavior. Confirm new layout tests fail against current UI.
- [x] 2. Reorganize activity presentations (`study-practice.tsx`, `study-revision-panel.tsx`, `guided-lessons.tsx`). Preserve props and scoring; mode radio selection + Start practice; disclosures for secondary information. Run existing quiz, revision and learning scripts.
- [x] 3. Rebuild workspace presentation (`study-workspace.tsx`, new inspector/scene-tools components, `study.css`, `typography.css`). Preserve action handlers and hidden mounted Library/Lessons. Header activity navigation, search scope, filter chips, stable browser, conditional inspector, single mobile sheet, wider management views, active scene indicators.
- [x] 4. Update `study-model.ts`, `anatomy.ts`, `scene.tsx` so transient panel layout updates frame fitting and labels, without being persisted. Add behavioral viewport assertions for inspector, activity width and mobile sheet states. Validate saved pose behavior after layout changes.
- [x] 5. Debug running app at desktop, 390×844, 320×568 and 844×390. Cover browser selection and scroll, tools state resets, practice setup/questions, library and review, keyboard and sheet navigation; inspect screenshots. Run `npm run check`, `npm run test:ui`, all existing `scripts/validate-*.mjs`, `npm run build`, diff review.
- [x] 6. Record validation and current local URL. Leave changes reviewable in the shared checkout.

## Execution notes
User approved the design in the prior response and explicitly asked to run locally. No additional design approval is needed. Local server is http://localhost:3016/.

### Local validation notes
- Browser regressions reproduced missing scope/inspector behavior, incorrect resize framing, and Explorer Escape retaining selected meshes before their fixes.
- All existing validate scripts pass after the restructure; production build and TypeScript pass.
- Final browser coverage includes all five study activities, scope/filters, inspector, keyboard focus, saved orbit, mobile widths and history, footer shortcuts, one-Escape deselection, and two-direction rotation with a single reset.
- Manual screenshots captured at 1440×900, 390×844, 320×568 and 844×390 in ignored `outputs/`.
- User's pre-existing README and maintenance/AGENTS edits are preserved. Changes stay in the shared checkout; local Vite stays running on port 3016.

Final result: 12 browser regressions passed in the full run (4.2 min), plus the added Hide/Restore regression passed separately (12.8 s). TypeScript, every existing validator, production build and diff whitespace checks passed. No runtime page errors in screenshot audit; search header/results share the same 350 px width and x position.

To repeat browser checks after `npm ci`, install the browser once with `npx playwright install chromium`, then run `npm run test:ui`. The configuration starts/reuses Vite on port 3016. Screenshots and traces for failures go to ignored `test-results/`.
